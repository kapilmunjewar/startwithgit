pip install virtualenv
virtualenv env //env - user defined folder
env\Scripts\activate.ps1
Set-ExecutionPolicy unrestricted

pip install flask
pip install flask-sqlalchemy
pip install openpyxl

//Usefull links
https://www.python.org/downloads/
https://flask.palletsprojects.com/en/2.0.x/quickstart/
https://flask-sqlalchemy.palletsprojects.com/en/2.x/quickstart/
https://inloop.github.io/sqlite-viewer/

//Usefull vscode extensions
jinja2 Snippet Kit
flask-snippets

//Create db
python
from app import db
db.create_all()

//Heroku deploy
1.create free account
https://signup.heroku.com/
2.download heroku cli for windows
https://devcenter.heroku.com/articles/heroku-cli
install it and set envi variable
3.create Procfile //heroku to know where to start 
web: gunicorn app:app

heroku login
git init
//if you don't have git download and install
https://git-scm.com/downloads
git add .
git commit -m "comment---"
heroku create git_repository_name //git_repository_name - whatever you want
git push heroku master

//want to push changes
git add .
git commit -m "comment---"
git push heroku master

git remote
//to check push and pull





//Learn Git for better project understanding

//create envirnment for deployment
pip install gunicorn
pip freeze > requirements.txt

//Learning git
git --version
git status
git init
git add .
git commit -m "version v1"
git log
git config user.name "kapilmunjewar"
git config user.email "kapilwebs@gmail.com"
git push origin master
git pull origin master
//remove changes from staging area
//coming out from editor mode press "q" q for quit
git status
git diff Notes.txt
git add .
git status
git diff Notes.txt
git diff --staged Notes.txt
git reset Notes.txt
git checkout Notes.txt  //reset single file
git checkout .  //reset all files

//Difference bitween git add ./-A/-u
git add -A  stages all
git add .	stages new and modified, without deleted
git -u 		stages modified and deleted, without new


//Generating SSH key commands
ssh-keygen -t ed25519 -C "kapilwebapps@gmail.com"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
clip < ~/.ssh/id_ed25519.pub  //copy key from cli to web

//Edit from Git website

